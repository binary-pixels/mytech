# Admin panel password
ADMIN_PASSWORD=your-password-here

# Vercel KV (Redis) — get from Vercel Dashboard → Storage → KV Database
# Run `npx vercel env pull .env.local` to auto-populate, or paste here:
# KV_URL=...
# KV_REST_API_URL=...
# KV_REST_API_TOKEN=...
# KV_REST_API_READ_ONLY_TOKEN=...
