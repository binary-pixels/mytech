# Admin panel password
ADMIN_PASSWORD=your-password-here

# Vercel KV — get these from Vercel Dashboard → Storage → KV Database
# Run `npx vercel env pull .env.local` to auto-populate, or paste them here:
# KV_URL=...
# KV_REST_API_URL=...
# KV_REST_API_TOKEN=...
# KV_REST_API_READ_ONLY_TOKEN=...
